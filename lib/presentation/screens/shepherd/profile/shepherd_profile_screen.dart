import 'package:flutter/material.dart';

import '../../../screens/shared/profile_screen.dart';

class ShepherdProfileScreen extends StatelessWidget {
  const ShepherdProfileScreen({super.key});

  @override
  Widget build(BuildContext context) => const ProfileScreen(role: 'shepherd');
}
