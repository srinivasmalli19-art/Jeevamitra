// STUB — replaced by lib/presentation/app/jeevamitra_app.dart
import 'package:flutter/material.dart';

class PasumitraApp extends StatelessWidget {
  const PasumitraApp({super.key});
  @override
  Widget build(BuildContext context) => const MaterialApp(home: SizedBox.shrink());
}
